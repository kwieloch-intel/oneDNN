--reset
# No mask
--dt=f32,f16,bf16
--batch=shapes_basic
# Buffer mask (bf16 excluded: GPU kernel bug)
--mask_type=buffer
--dt=f32,f16
--batch=shapes_basic
# Causal mask
--mask_type=causal_top_left
--dt=f32,f16
--batch=shapes_basic
# Scale (reset mask)
--mask_type=none
--scale_type=mul
--dt=f32,f16
--batch=shapes_basic
# Transformer shapes (GPU-safe, no seq_q=1)
--reset
--dt=f16,bf16
--batch=shapes_transformer_gpu

# Forward training (needed for backward; verifies DST with training prop)
--reset
--dir=FWD_D
--dt=f16,bf16
--batch=shapes_transformer_gpu

# Backward
--reset
--dir=BWD_D
--dt=f16,bf16
--batch=shapes_bwd
--mask_type=causal_top_left
--dt=f16
--batch=shapes_bwd
